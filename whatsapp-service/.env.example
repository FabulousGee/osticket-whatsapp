# WhatsApp Service Configuration

# Port for the REST API
PORT=3000

# Host to bind to (use 127.0.0.1 for local only, 0.0.0.0 for all interfaces)
HOST=127.0.0.1

# Webhook URL - where to send incoming messages (your osTicket installation)
WEBHOOK_URL=https://your-osticket.example.com/api/whatsapp-webhook.php

# Webhook secret token for authentication
WEBHOOK_SECRET=your-secret-token-here

# Log level (trace, debug, info, warn, error, fatal)
LOG_LEVEL=info

# Log directory for service wrapper scripts (optional)
# LOG_DIR=C:\path\to\logs
